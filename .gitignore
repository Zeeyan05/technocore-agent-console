# dependencies
/node_modules
/.pnp
.pnp.js
.yarn/install-state.gz

# testing
/coverage

# next.js
/.next/
/out/

# production
/build
/dist

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# local env files
.env*.local

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts

# agent tooling — local only, not part of the project
# AGENTS.md and CLAUDE.md are written by `next dev` on every start
# (node_modules/next/dist/server/lib/generate-agent-files.js), so they are
# ignored rather than committed: they reappear on disk but stay out of the tree.
/AGENTS.md
/CLAUDE.md
/.claude/
